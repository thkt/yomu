{
  "seeded_floor": 0.9,
  "measured_mean": 0.911458,
  "model_hash": "stub-test-support",
  "gate1_index_seconds": 3,
  "_note": "Gate1 committed threshold (#128 Phase 3). measured_mean is the mean seeded recall observed by tests/brief_recall.rs over vendor/rurico@1d6650a and vendor/amici@ae8c068 with the stub embedder (deterministic: the seeded brief path is graph-only, verified bit-identical across processes and re-indexes). seeded_floor stays below measured_mean, but with 16 entries one entry shifting a notch (~0.016 of mean) can land near the floor — treat a marginal failure as a prompt to read the per-entry RECALL lines, not to bump the floor. Ratchet seeded_floor up as recall improves. model_hash records the embedder identity (stub) rather than a semantic model hash; the real-model hash belongs to the Gate2 seed-less baseline (Phase 5). Known closure limits keep four entries below 1.0: src/bin/eval_harness.rs imports the lib via the package-name path (use amici::...), which the resolver does not resolve to an internal edge (#242), costing amici-ir-metrics, amici-baseline-snapshot-schema and amici-quality-regression-detection (0.75 each); amici-scoring-against-known-answers (0.333) additionally needs eval/fixture.rs, which from eval/metrics.rs is reachable only by mixing a backward hop (mod parent eval.rs) with a forward hop, a path the direction-monotone closure does not traverse (measured in #250 Phase 1b). Per AS-004 the must-include sets stay as domain reasoning dictates; these gaps are measurements of brief's closure limits, not corpus defects."
}
